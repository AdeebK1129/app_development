// import the component structure for Angular
import { Component } from "@angular/core";

// declare the use of Component in this file
@Component({
    standalone: true,
    selector: 'products', // the tag used to load the page
    templateUrl: './products.component.html' // loading page for 'products' component
})

export class ProductComponent{

}